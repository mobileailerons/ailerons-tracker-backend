<label for="{{ input_id }}">
    {{ label }}
    <input type="file"
           class="file"
           id="{{ input_id }}"
           name="{{ input_id }}"
           accept="{{ accept }}" />
</label>
